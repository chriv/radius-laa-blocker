# Host inventory. One section per deployment target.
# Per-host secrets and overrides live in hosts/<host>/.env (gitignored).
# See hosts/<host>/.env.example for all available keys.

[host-1]
deploy_method = caprover
arch = aarch64
environment = production

[host-2]
deploy_method = caprover
arch = aarch64
environment = development
